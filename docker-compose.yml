version: '3.8'

services:
  wsi-studio:
    build: .
    container_name: wsi-stitching-studio
    ports:
      - "127.0.0.1:5000:5000"
    volumes:
      - ./data:/app/data
      - ./sample_data:/app/sample_data
    environment:
      - PORT=5000
    restart: unless-stopped
